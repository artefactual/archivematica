---
version: "2.1"

services:
  archivematica-tests:
    image: "archivematica-tests:latest"
    build:
      context: "../"
      dockerfile: "hack/Dockerfile"
      args:
        TARGET: "archivematica-tests"
    volumes:
      - "../:/src"
      - "../cov:/src/cov"
    links:
      - "mysql"
